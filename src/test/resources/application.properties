# src/test/resources/application.properties
spring.datasource.url= # Leave blank or use a mock connection string
spring.datasource.username= # Leave blank
spring.datasource.password= # Leave blank
# Disable database initialization if not needed
spring.sql.init.mode=never
